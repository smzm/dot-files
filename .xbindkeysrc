###########################
# xbindkeys configuration #
###########################
#
# Version: 0.1.3
#
# If you edit this, do not forget to uncomment any lines that you change.
# The pound(#) symbol may be used anywhere for comments.
#
# A list of keys is in /usr/include/X11/keysym.h and in
# /usr/include/X11/keysymdef.h 
# The XK_ is not needed. 
#
# List of modifier (on my keyboard): 
#   Control, Shift, Mod1 (Alt), Mod2 (NumLock), 
#   Mod3 (CapsLock), Mod4, Mod5 (Scroll). 
#
# Another way to specifie a key is to use 'xev' and set the 
# keycode with c:nnn or the modifier with m:nnn where nnn is 
# the keycode or the state returned by xev 
#
# This file is created by xbindkey_config 
# The structure is : 
# # Remark 
# "command" 
# m:xxx + c:xxx 
# Shift+... 




#keystate_numlock = enable
#keystate_scrolllock = enable
#keystate_capslock = enable



# --- Media Controls using playerctl ---
# All bindings use only the NumLock (Mod2) modifier + arrow or numpad keys

# ▶️ Seek forward 5 seconds
# Press NumLock + → (Right Arrow)
"playerctl position 5+"
    m:0x0 + c:85
    KP_Right

# ⏪ Seek backward 5 seconds
# Press NumLock + ← (Left Arrow)
"playerctl position 5-"
    m:0x0 + c:83
    KP_Left

# 🔊 Increase volume by 5%
# Press NumLock + ↑ (Up Arrow)
"playerctl volume 0.05+"
    m:0x00 + c:80
    KP_Up

# 🔉 Decrease volume by 5%
# Press NumLock + ↓ (Down Arrow)
"playerctl volume 0.05-"
    m:0x0 + c:88
    KP_Down

# ⏯️ Toggle play/pause
# Just press the Pause key (no modifiers)
"playerctl play-pause"
    m:0x0 + c:84
    KP_Begin

# ⏭️ Next track
# Press NumLock + Next (usually PgDn or keypad 3)
"playerctl next"
    m:0x0 + c:81
    KP_Prior

# ⏮️ Previous track
# Press NumLock + Prior (usually PgUp or keypad 9)
"playerctl previous"
    m:0x0 + c:79
    KP_Home

# End of xbindkeys configuration
