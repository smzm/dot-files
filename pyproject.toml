[project]
name = "dot-files"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "smzm", email = "smzm@github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "inquirer>=3.4.1",
    "rich>=15.0.0",
]

[project.scripts]
dot-files = "dot_files:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
